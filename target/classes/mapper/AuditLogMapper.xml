<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.factory.repair.mapper.AuditLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (entity_type, entity_id, action, operator_id, operator_name, before_data, after_data, extra_info)
        VALUES (#{entityType}, #{entityId}, #{action}, #{operatorId}, #{operatorName}, #{beforeData}, #{afterData}, #{extraInfo})
    </insert>
    <select id="selectByEntity" resultType="AuditLog">
        SELECT * FROM audit_log WHERE entity_type = #{entityType} AND entity_id = #{entityId} ORDER BY created_at DESC
    </select>
    <select id="selectByOperator" resultType="AuditLog">
        SELECT * FROM audit_log WHERE operator_id = #{operatorId} ORDER BY created_at DESC
    </select>
</mapper>
